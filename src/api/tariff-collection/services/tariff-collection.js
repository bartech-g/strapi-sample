'use strict';

/**
 * tariff-collection service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tariff-collection.tariff-collection');
